{
  "name": "secure-convert-client",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Static client for SecureConvert — serve with auto-open",
  "scripts": {
    "dev": "node ./dev-serve.js",
    "dev:python": "node ./dev-open.js",
    "start": "python3 -m http.server 5500",
    "serve": "serve . -l 5500"
  },
  "dependencies": {
    "serve": "^14.2.5"
  }
}
